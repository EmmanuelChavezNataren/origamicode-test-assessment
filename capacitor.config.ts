import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.origamicode.testassessment.ecn',
  appName: 'TestAssessmentOrigamiCode',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
